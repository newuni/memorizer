# ROADMAP

## Near-term
- Packaging + global install (`pipx`) for `memorizer` CLI.
- Keep expanding tests + CI coverage thresholds.
- Improve performance defaults for retrieval + reranking.

## Mid-term
- Metadata filters in retrieval.
- Better observability (metrics + tracing).
- Optional connectors/importers (web, docs, chat).

## Future explorations
- **Autonomous agent memory loop** (a.k.a. `chat-loop` concept):
  - useful for autonomous agents that should self-manage retrieval + write-back;
  - currently **deferred**, not prioritized for coding-agent workflows.
